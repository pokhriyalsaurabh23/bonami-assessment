{
  "name": "comment-server",
  "version": "1.0.0",
  "description": "Server application for comments and authors",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "prisma generate",
    "migrate": "prisma migrate dev",
    "seed": "node src/seed.js",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@prisma/client": "^4.6.1",
    "body-parser": "^1.20.1",
    "dotenv": "^16.1.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^2.0.22",
    "prettier": "^3.3.1"
  }
}
